version: '3.8'

services:
  aegisa:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - FAUCET_PRIVATE_KEY=your_private_key_here
      - IOTA_MNEMONIC=your_iota_mnemonic_here
    restart: always
